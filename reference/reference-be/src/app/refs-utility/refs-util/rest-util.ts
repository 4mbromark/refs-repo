import bodyParser from "body-parser";

export class RestUtil {

    public static jsonParser = bodyParser.json();

    public static sendIfExists(o: Object) {
        if (o) {

        } else {
            
        }
    }
}